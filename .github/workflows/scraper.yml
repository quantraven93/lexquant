name: Update Cases

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        # `npm install` rather than `npm ci` — same fix as test.yml: the
        # lockfile is generated on macOS and misses Linux-only optional
        # deps (@emnapi/*), which makes `npm ci` hard-fail on ubuntu.
        run: npm install --no-audit --no-fund

      - name: Run case update script
        run: npx tsx scripts/update-cases.ts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          ECOURTS_API_KEY: ${{ secrets.ECOURTS_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}

      - name: Notify webhook
        if: always()
        run: |
          curl -s -X POST "${{ secrets.VERCEL_URL }}/api/webhook" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -H "Content-Type: application/json" \
            -d '{"timestamp":"'"$(date -u +%Y-%m-%dT%H:%M:%SZ)"'","status":"${{ job.status }}"}'
        continue-on-error: true
